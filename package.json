{
  "name": "web_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm install && cd ../web_server && npm install",
    "build": "cd client && npm run build",
    "buildDev": "cd client && npm run buildDev",
    "start": "cd web_server && node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-script": "^2.0.5"
  },
  "engines": {
    "node": "10.19.0",
    "npm": "6.14.4"
  }
}
